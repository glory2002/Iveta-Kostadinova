
  # Design landing page layout

  This is a code bundle for Design landing page layout. The original project is available at https://www.figma.com/design/dxwf83oUzzu8oTRcqriS3I/Design-landing-page-layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  